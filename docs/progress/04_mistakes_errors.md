# Mistakes & Errors Log
- **Error 1**: `cargo: command not found`.
  - Fix: Installed `rustup` via curl script and verified stable toolchain.
- **Warnings**: Unused imports/fields in stub files (`config.rs`, `providers/*.rs`).
  - Fix: Ignored for now. Will be resolved when implementation details are added.
