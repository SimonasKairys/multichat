# Authoritative Status

`01`–`14` are plans, not specs; README security posture wins.

**Shipped**: provider routing; model selection; `●/○/×` readiness;
keyring keys; encrypted vault; hard-link/symlink guards; anchored MAC audit; bounded
prompts, replies, and stream lines; `--classified`; Linux `mlockall`; skills,
project read/list/write + approval; delegation and failure ledger; SOCKS5; CI/mutants.

**Not implemented**: seccomp, TLS pinning, non-Linux process lock, clipboard.
