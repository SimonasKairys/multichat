[
  {
    "diff": "--- src/audit.rs\n+++ replace unhex -> Result<Vec<u8>> with Ok(vec![])\n@@ -862,26 +862,17 @@\n /// length could still be even while a 2-byte cut lands inside a multi-byte\n /// codepoint, and `&str` indexing panics with \"byte index is not a char boundary\"\n /// rather than returning an error. This crate has shipped exactly this failure class\n /// before (fixed in 923b934) — the fix here is the same principle: work on\n /// `as_bytes()` throughout so there is no `&str` index to ever land wrong, and let\n /// any byte outside `0-9a-fA-F` (which includes every continuation/lead byte of a\n /// multi-byte UTF-8 sequence) fail the digit match cleanly instead of panicking.\n fn unhex(s: &str) -> Result<Vec<u8>> {\n-    let bytes = s.as_bytes();\n-    if !bytes.len().is_multiple_of(2) {\n-        return Err(anyhow!(\"odd-length hex string\"));\n-    }\n-    bytes\n-        .as_chunks::<2>()\n-        .0\n-        .iter()\n-        .map(|pair| Ok((hex_digit(pair[0])? << 4) | hex_digit(pair[1])?))\n-        .collect()\n+    Ok(vec![]) /* ~ changed by cargo-mutants ~ */\n }\n \n /// A single ASCII hex digit's value, or an error for anything else — including any\n /// byte of a multi-byte UTF-8 sequence, all of which fall outside `0-9a-fA-F`.\n fn hex_digit(b: u8) -> Result<u8> {\n     match b {\n         b'0'..=b'9' => Ok(b - b'0'),\n         b'a'..=b'f' => Ok(b - b'a' + 10),\n",
    "file": "src/audit.rs",
    "function": {
      "function_name": "unhex",
      "return_type": "-> Result<Vec<u8>>",
      "span": {
        "end": {
          "column": 2,
          "line": 880
        },
        "start": {
          "column": 1,
          "line": 856
        }
      }
    },
    "genre": "FnValue",
    "name": "src/audit.rs:870:5: replace unhex -> Result<Vec<u8>> with Ok(vec![])",
    "package": "simon",
    "replacement": "Ok(vec![])",
    "span": {
      "end": {
        "column": 19,
        "line": 879
      },
      "start": {
        "column": 5,
        "line": 870
      }
    }
  },
  {
    "diff": "--- src/audit.rs\n+++ replace unhex -> Result<Vec<u8>> with Ok(vec![0])\n@@ -862,26 +862,17 @@\n /// length could still be even while a 2-byte cut lands inside a multi-byte\n /// codepoint, and `&str` indexing panics with \"byte index is not a char boundary\"\n /// rather than returning an error. This crate has shipped exactly this failure class\n /// before (fixed in 923b934) — the fix here is the same principle: work on\n /// `as_bytes()` throughout so there is no `&str` index to ever land wrong, and let\n /// any byte outside `0-9a-fA-F` (which includes every continuation/lead byte of a\n /// multi-byte UTF-8 sequence) fail the digit match cleanly instead of panicking.\n fn unhex(s: &str) -> Result<Vec<u8>> {\n-    let bytes = s.as_bytes();\n-    if !bytes.len().is_multiple_of(2) {\n-        return Err(anyhow!(\"odd-length hex string\"));\n-    }\n-    bytes\n-        .as_chunks::<2>()\n-        .0\n-        .iter()\n-        .map(|pair| Ok((hex_digit(pair[0])? << 4) | hex_digit(pair[1])?))\n-        .collect()\n+    Ok(vec![0]) /* ~ changed by cargo-mutants ~ */\n }\n \n /// A single ASCII hex digit's value, or an error for anything else — including any\n /// byte of a multi-byte UTF-8 sequence, all of which fall outside `0-9a-fA-F`.\n fn hex_digit(b: u8) -> Result<u8> {\n     match b {\n         b'0'..=b'9' => Ok(b - b'0'),\n         b'a'..=b'f' => Ok(b - b'a' + 10),\n",
    "file": "src/audit.rs",
    "function": {
      "function_name": "unhex",
      "return_type": "-> Result<Vec<u8>>",
      "span": {
        "end": {
          "column": 2,
          "line": 880
        },
        "start": {
          "column": 1,
          "line": 856
        }
      }
    },
    "genre": "FnValue",
    "name": "src/audit.rs:870:5: replace unhex -> Result<Vec<u8>> with Ok(vec![0])",
    "package": "simon",
    "replacement": "Ok(vec![0])",
    "span": {
      "end": {
        "column": 19,
        "line": 879
      },
      "start": {
        "column": 5,
        "line": 870
      }
    }
  },
  {
    "diff": "--- src/audit.rs\n+++ replace unhex -> Result<Vec<u8>> with Ok(vec![1])\n@@ -862,26 +862,17 @@\n /// length could still be even while a 2-byte cut lands inside a multi-byte\n /// codepoint, and `&str` indexing panics with \"byte index is not a char boundary\"\n /// rather than returning an error. This crate has shipped exactly this failure class\n /// before (fixed in 923b934) — the fix here is the same principle: work on\n /// `as_bytes()` throughout so there is no `&str` index to ever land wrong, and let\n /// any byte outside `0-9a-fA-F` (which includes every continuation/lead byte of a\n /// multi-byte UTF-8 sequence) fail the digit match cleanly instead of panicking.\n fn unhex(s: &str) -> Result<Vec<u8>> {\n-    let bytes = s.as_bytes();\n-    if !bytes.len().is_multiple_of(2) {\n-        return Err(anyhow!(\"odd-length hex string\"));\n-    }\n-    bytes\n-        .as_chunks::<2>()\n-        .0\n-        .iter()\n-        .map(|pair| Ok((hex_digit(pair[0])? << 4) | hex_digit(pair[1])?))\n-        .collect()\n+    Ok(vec![1]) /* ~ changed by cargo-mutants ~ */\n }\n \n /// A single ASCII hex digit's value, or an error for anything else — including any\n /// byte of a multi-byte UTF-8 sequence, all of which fall outside `0-9a-fA-F`.\n fn hex_digit(b: u8) -> Result<u8> {\n     match b {\n         b'0'..=b'9' => Ok(b - b'0'),\n         b'a'..=b'f' => Ok(b - b'a' + 10),\n",
    "file": "src/audit.rs",
    "function": {
      "function_name": "unhex",
      "return_type": "-> Result<Vec<u8>>",
      "span": {
        "end": {
          "column": 2,
          "line": 880
        },
        "start": {
          "column": 1,
          "line": 856
        }
      }
    },
    "genre": "FnValue",
    "name": "src/audit.rs:870:5: replace unhex -> Result<Vec<u8>> with Ok(vec![1])",
    "package": "simon",
    "replacement": "Ok(vec![1])",
    "span": {
      "end": {
        "column": 19,
        "line": 879
      },
      "start": {
        "column": 5,
        "line": 870
      }
    }
  }
]